<script setup lang="ts">import {Head,router} from '@inertiajs/vue3'; import PageHeader from '@/Components/Shared/PageHeader.vue'; import ShowSection from '@/Components/Show/ShowSection.vue'; import {Button} from '@/components/ui/button'; defineProps<{items:any}>();</script>
<template><Head title="AI Generation History"/><div class="space-y-6 p-6"><PageHeader title="AI Generation History" description="Audit the inputs, outputs, model, status, policies, and prompt examples used by AI Content Studio."/><ShowSection title="Generation activity"><div class="divide-y rounded-xl border"><article v-for="item in items.data" :key="item.id" class="p-4"><div class="flex items-center justify-between"><div><p class="font-semibold">{{item.feature.replaceAll('_',' ')}}</p><p class="text-xs text-muted-foreground">{{item.provider||'—'}} · {{item.model||'—'}} · {{item.created_at}}</p></div><span class="rounded-full bg-muted px-2 py-1 text-xs">{{item.status}}</span></div><p class="mt-3 text-sm"><strong>Input:</strong> {{item.input_text}}</p><p v-if="item.output_text" class="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{{item.output_text}}</p><p v-if="item.error_message" class="mt-2 text-sm text-destructive">{{item.error_message}}</p></article></div><div class="mt-4 flex gap-2"><Button v-for="link in items.links" :key="link.label" size="sm" variant="outline" :disabled="!link.url" @click="link.url&&router.visit(link.url)" ><span v-html="link.label"></span></Button></div></ShowSection></div></template>
